<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <link rel="stylesheet" href="css/transaction.css">
    <script src="https://code.jquery.com/jquery-3.5.0.js"></script>
    <title>Bank</title>
</head>
<body>
   
    <header>
        <%- include('../partials/header') %>
    </header>
    <div class="alert alert-warning alert-dismissible fade show myclass" role="alert">
        <strong>Transaction SuiccesFull!</strong>check your account balance in the customers list.
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
    </div>

    <script>
        function myfunction(){
            $( ".myclass" ).css( "visibility", "visible" );
        }
    </script>
    
    <h4 class="transaction_header">Select customer to transfer to</h4>  
        <div class="transactioncard">
            <% for(var i=0;i<userdata.length;i++) { %>
                <div class=" transactiondetails">
                    <h5><%= userdata[i].name%></h5>
                    <form action="/send" method="POST">
                        <input type="hidden" name="sender" value="<%=data.name%>"> 
                        <input type="hidden" name="amount" value="<%= amount %>">
                        <input type="hidden" name="reciever" value="<%= userdata[i].name %>">
                        <button type="submit" onclick="myfunction()" class="btn btn-success">Send</button>
                    </form>
                </div>
            <% } %>
        </div>
    <footer>
        <%- include('../partials/footer') %>
    </footer>

    
</body>
</html>